# 物品使用记录101

这是“物品使用记录101”教程页的 GitHub Pages 发布文件。

使用方法：
1. 将本文件夹内的 `index.html` 上传到 GitHub 仓库根目录。
2. 在 GitHub 仓库 Settings -> Pages 中选择 `Deploy from a branch`，分支选择 `main`，目录选择 `/root`。
3. 发布后访问 `https://Ketonsi.github.io/item-usage-101/`。
